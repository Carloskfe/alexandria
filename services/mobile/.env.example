# ─── Mobile (Expo) ────────────────────────────────────────────────────────────
# API base URL. In development points directly to the NestJS container/server.
# In production replace with your deployed API URL (e.g. https://api.noetia.app).
EXPO_PUBLIC_API_URL=http://localhost:4000
